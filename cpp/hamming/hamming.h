#if !defined(HAMMING_H)
#define HAMMING_H

#include <string>
#include <algorithm>
#include <cmath>

namespace hamming {
    int compute(std::string s1, std::string s2);
}  // namespace hamming

#endif // HAMMING_H
