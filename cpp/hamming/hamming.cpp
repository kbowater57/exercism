#include "hamming.h"

namespace hamming {
    int compute(std::string s1, std::string s2)
    {
        if(s1 == s2)
        {
            return 0;
        }
        return {};
    }
}  // namespace hamming
