#include "sum_of_multiples.h"

namespace sum_of_multiples {
    int to(std::list<int> multiples, int limit)
    {
        ++multiples.begin();
        ++limit;
        return 0;
    }
}  // namespace sum_of_multiples
